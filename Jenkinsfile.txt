pipeline {
  agent any
  environment {
    CLOUDSDK_CORE_PROJECT='deai-2025'
    CLIENT_EMAIL='wd34-sa@deai-2025.iam.gserviceaccount.com'
    GCLOUD_CREDS=credentials('deai-project-id')
  }
  stages {
    stage('copy_to_gcp') {
      steps {
        sh '''
          ls -lrt
          gcloud auth activate-service-account --key-file="$GCLOUD_CREDS"
          gsutil cp *.py gs://com-data-learn1-we45-2/
          echo 'File Copied into GCS bucket'
        '''
      }
    }
  }
}
